#include <stdio.h> 
#include <string.h>
#include <stdlib.h>

int countWord(char filename[]) {
	FILE* f = fopen(filename, "r"); //Filename is relative to the cwd

	int i = 0;
	while(fprintf(f, "%s\n")){
		i++;
	}

	return i;
}
